# Task 44: 화면 상태 정합성 — 실패와 낡음 처리

> 미착수. Task 42(실패 정규화) 완료가 전제다.

## 문제

화면이 실제와 다른 상태를 사실처럼 보여주는 경우가 둘 있다.
증상은 달라 보이지만 **"화면이 거짓말을 한다"는 같은 문제**이고, 해법도 같다 — 다시 가져오기.

| | 어긋남 | 사용자가 보는 것 |
|---|---|---|
| **A. 실패를 빈 화면으로** | 조회 실패인데 빈 배열 반환 | "참여중인 Flag가 없습니다" |
| **B. 낡은 화면 유지** | 409인데 문구만 띄움 | 이미 무효한 초대가 목록에 그대로 |

### A — 조회 실패 15곳

```ts
catch { return { success: false as const, data: [] as FlagResult[] }; }
```

```
flag.ts:21,31,44,54,159,169,236,248,290
buzz.ts:21,134   label.ts:15,27   notification.ts:15   social.ts:100
```

사용자는 빈 화면을 사실로 믿고, 재시도할 방법도 뭔가 잘못됐다는 신호도 없다.
`{ silent: true }`가 붙은 곳은 로그조차 남지 않는다.

Task 41 검증에서 실제로 이 함정에 걸렸다. 프로필의 최근 Flag가 비어 보였는데
화면만으로는 "조회 실패"인지 "그 친구에게 Flag가 없음"인지 알 수 없어,
API를 직접 호출해서야 후자임을 확인했다.

### B — 409를 문구로만 처리

409 Conflict는 **"당신이 보고 있는 데이터가 이미 바뀌었다"**는 뜻이다.
안내를 읽은 뒤에도 화면은 여전히 틀린 상태다. Task 41에서 확인된 사례:

| 상황 | 지금 |
|---|---|
| 종료된 Flag에서 참여 취소 | 문구 표시. 참여자 목록은 그대로 |
| 만료된 초대 수락 | 문구 표시. **초대 카드가 목록에 남아 다시 누를 수 있다** |

## 왜 하나로 묶는가

A의 **재시도**와 B의 **재조회**는 같은 장치다. 따로 만들면 두 벌이 되고
모양이 어긋난다. 대상도 겹친다 — Flag 상세 화면은 A(댓글·메모리얼 조회 실패)와
B(참여 취소 409) 양쪽에 걸린다.

## 방향

`ApiError.kind`와 `status`로 갈라 화면이 다르게 반응한다.

| 상황 | 화면 |
|---|---|
| `kind: network` · `timeout` | "연결을 확인해 주세요" + **재시도** |
| `kind: http` · `parse` | 사유 표시. 재시도는 상황에 따라 |
| 성공 + 빈 배열 | 지금의 빈 상태 문구 (진짜 "없음") |
| `status: 409` | **재조회 → 화면 갱신 → 그다음 안내** |

## Phase 구성

회귀가 났을 때 원인을 가릴 수 있도록 장치와 적용을 나눈다.

1. **재조회·재시도 장치** — 공통 UI 프리미티브를 먼저 만들고 커밋
2. **A 적용** — 조회 실패 15곳을 `kind` 기반으로 전환
3. **B 적용** — 409 재조회. 선행 확인(아래)이 끝난 뒤 마지막에

B를 마지막에 두는 이유는 아래 미확인 항목이 A의 착수를 막지 않게 하기 위해서다.

## 착수 전 결정할 것

**실패 상태 UI 패턴.** 액션만 고쳐서는 끝나지 않는다 —
전체 화면인지 인라인인지, 재시도 버튼을 어디 두는지,
**부분 실패**를 어떻게 표현하는지 정해야 한다.

특히 [app/page.tsx](src/app/page.tsx)는 `Promise.all` + 개별 `.catch()`로
여러 조회를 병렬 처리한다. 하나만 실패했을 때의 표현이 별도 설계 대상이다.

## 미확인 사항 (Phase 3 선행)

**모든 409가 "재조회하면 해결"인 것은 아니다.** 409를 내는 엔드포인트와
각각의 의미를 먼저 정리해야 한다 — "이미 처리됨"과 "상태가 안 맞음"은 대응이 다를 수 있다.

`code`(예외 클래스명)로 구분하고 싶어질 수 있으나 **그 방향으로 가지 않는다.**
클래스명은 계약이 아니라 구현 세부다(Task 42 참고). 구분이 정말 필요하면
백엔드에 안정적인 code 계약을 요청한다.

## 대상 후보 (B)

- [FlagDetail.tsx](src/components/Flag/FlagDetail.tsx) — 참여 취소, 참여하기
- [FlagInvitationList.tsx](src/components/Flag/FlagInvitationList.tsx) — 초대 수락·거절
- 그 밖에 낙관적 업데이트를 하는 곳 전반 (되돌리기도 함께 검토)

## Result

**완료** (2026-08-21) — `agent/task-44-screen-state-integrity`

### Phase 1 — 장치 (화면 동작 무변경, 16/16)

`Failure` 타입 + `toFailure()`, `components/common/EmptyState.tsx`(지역 함수 승격),
`components/common/FailureState.tsx`(신규), 조회 액션 15곳에 `failure` 필드 추가.

### Phase 2 — 적용 (스텁 10/10, 실제 백엔드 회귀 16/16)

| 화면 | 조회 실패 시 |
|---|---|
| 메인 그래프 | `FailureState`. 라벨·알림수는 부수라 조용히 넘어감 |
| Flag 목록 | 탭별 분리. 둘러보기는 클라이언트 조회라 `onRetry` 로 해당 상태만 복구 |
| 초대 목록 | 받은/보낸 각각. 한쪽 실패가 다른 쪽을 가리지 않음 |
| 댓글 | 인라인 실패 + 재시도 |
| 메모리얼 | 실패가 "잠김"보다 우선 |

핵심 대조 확인:

```
조회 실패      → "일시적인 오류가 발생했습니다" + [다시 시도]
성공 + 빈 배열 → "아직 연결된 친구가 없습니다" (기존 문구 유지)
```

예상 못 한 발견: 메모리얼은 조회 실패 시 `locked = true` 가 되어 **잠김 안내**가 떴다.
장애를 권한 문제로 오인시키는 케이스라 실패 표시를 우선하도록 했다.

### Phase 3 (409 재조회) — 불필요로 판정, 착수하지 않음

**전제가 성립하지 않았다.** "409 = 화면이 낡음 → 재조회하면 해결"이라고 설계했으나
대상 두 곳 모두 재조회할 것이 없었다.

실측(만료 초대):

```
마감 전 받은 초대: 1개 (포함)
마감 후 받은 초대: 1개 (포함)     ← 서버가 계속 준다
수락 → 409 "모집 기간이 지난 깃발입니다."
수락 실패 후에도 목록에 남아 있음  ← 재조회해도 같은 카드가 돌아온다
```

참여 취소도 마찬가지다. 409는 "종료 후엔 취소 불가"인데 **화면은 이미 맞다** —
사용자는 실제로 참여자이고 Flag 는 실제로 종료됐다. 낡은 것이 없으니 재조회할 것도 없다.

**409 자체는 이미 처리돼 있다.** Task 41에서 고정 문구 덮어쓰기를 제거하면서
사용자에게 사유가 표시되고 있고, Task 42·43 검증에서 매번 확인됐다.

남는 것은 에러 처리가 아니라 **"누를 수 없는 버튼을 미리 감추기"** 다.
`ReceivedFlagInvitationResult` 에 마감·상태 필드가 없어 카드가 렌더 시점에
만료 여부를 알 수 없다. 백엔드가 초대 목록에 Flag 상태를 싣거나 만료된 초대를
빼주는 계약 변경이 선행돼야 하므로, 이 태스크 범위 밖으로 둔다.

### 교훈

Phase 3 착수 직전에 "전수 수집은 생략해도 된다"고 판단했으나, 그 생략하려던
확인이 전제를 깼다. 확인된 것은 "409 가 난다"였지 "재조회가 해결한다"가 아니었다.
**동작을 바꾸기 전에 그 동작이 실제로 무엇을 바꾸는지 먼저 재어볼 것.**
