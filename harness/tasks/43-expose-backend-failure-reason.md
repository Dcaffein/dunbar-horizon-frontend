# Task 43: 백엔드 실패 사유 노출 — 고정 문구 덮어쓰기 제거

> 구현 절차·검증은 `PLAN.md` 참고.

## 배경

Server Action 29곳이 백엔드가 준 실패 사유를 받아놓고 **고정 문구로 덮어쓴다.**

```ts
catch (error) {
  if (isRedirectError(error)) throw error;
  return { success: false as const, message: "Flag 삭제에 실패했습니다." };
  //                                          ↑ 왜 실패했는지가 사라진다
}
```

사용자는 이유를 알 수 없고, 문의가 들어와도 재현할 수 없다.
백엔드는 이미 표시용 한국어 문장을 주고 있는데(`"모집 기간이 지난 깃발입니다."`)
프론트가 문 앞에서 버리는 구조다.

Task 41에서 같은 성격의 2곳(`leaveAction`, `acceptInvitationAction`)을 고쳐
실제로 사유가 화면에 뜨는 것을 확인했다. 이 태스크는 나머지에 같은 처리를 한다.

## 왜 지금인가 — Task 42 다음이어야 했다

이 작업은 기술적으로는 42 없이도 가능했다. 그러나 **그 순서로 했으면 안 됐다.**

42 이전에는 `error.message`가 서버 문장일 수도, `fetch failed` 같은 JS 런타임
문자열일 수도 있었다. 그 상태에서 통과 범위를 넓혔다면 영문 내부 문자열 노출 지점을
**20곳에서 49곳으로 늘리는** 작업이 됐다.

42가 세운 불변식(`ApiError.message`는 언제나 보여도 되는 문장) 위에서만
이 변경이 안전하다. 5xx·네트워크·파싱 실패는 이미 FE 고정 문구로 치환되므로,
통과시켜도 사용자에게 도달하는 것은 4xx의 서버 문장뿐이다.

## 대상

| 파일 | 곳수 |
|---|---|
| `buzz.ts` | 7 |
| `flag.ts` | 8 |
| `profile.ts` · `label.ts` | 3 · 3 |
| `friendRequest.ts` · `friendship.ts` · `notification.ts` · `social.ts` | 2씩 |

이 중 **12곳은 조회(GET) 실패**로 `"...불러오는 데 실패했습니다"` 형태다.
페이지가 그 `message`를 렌더링하지 않는 경우가 많아 체감 효과는 낮지만,
파일 안에서 처리 형태를 통일하는 편이 다음 사람이 읽기 쉽다. 전부 동일하게 바꾼다.

체감 효과는 **나머지 17곳(변경 요청)** 에서 나온다 — 삭제·수정·초대·권한 변경 등
사용자가 버튼을 눌러 실패를 직접 마주하는 지점들이다.

## 범위

### 포함

- 29곳을 `error instanceof Error ? error.message : "<기존 문구>"` 형태로 통일
- 기존 고정 문구는 **fallback으로 유지**한다(`ApiError`가 아닌 예외가 올라올 경우)

### 제외

| 항목 | 이유 |
|---|---|
| 조회 실패 시 빈 배열 반환 15곳 | **Task 44.** "실패"와 "데이터 없음" 구분은 `kind`와 화면 설계가 필요하다 |
| 409 → 재조회 | **Task 44.** 문구가 아니라 동작 변경. 위와 같은 "다시 가져오기" 장치를 쓴다 |
| `validation` 필드 바인딩 | **Task 45.** 폼 UI 변경 |
| 컴포넌트 쪽 fallback 문구 | 그대로 둔다. `result.message ?? "기본 문구"` 구조 유지 |

## 리스크

**백엔드 문구가 그대로 사용자에게 도달하는 범위가 29곳 늘어난다.**
4xx 문구는 표시용 한국어임을 실측으로 확인했지만(Task 41·42),
확인된 것은 일부 엔드포인트다. 아직 보지 못한 4xx 문구가 어색하거나
내부 용어를 담고 있을 수 있다.

완화책은 둘이다 — 42가 5xx·네트워크를 이미 막고 있고,
검증에서 도메인별로 실제 실패를 한 번씩 일으켜 문구를 눈으로 확인한다.

## 착수 후 드러난 것 — 404는 통과시키면 안 된다

「리스크」로 적어둔 "아직 못 본 4xx 문구가 어색할 수 있다"가 **실제로 터졌다.**
사용자에게 도달하게 될 문구를 도메인별로 전수 수집한 결과:

| status | 문구 | 판정 |
|---|---|---|
| 403 | 호스트 권한이 없습니다. | 양호 |
| 409 | 모집 기간이 지난 깃발입니다. | 양호 |
| 400 | 입력값이 올바르지 않습니다. | 양호 |
| **404** | 존재하지 않는 flag : 99999999 | 영문 엔티티명 + 내부 ID |
| **404** | 존재하지 않는 flagMemorial / flagComment / flagParticipant | 동일 |
| **404** | 해당 id를 가진 label을 찾을 수 없습니다 : 99999999 | 동일 |
| **404** | User(4)와 User(99999999) 사이의 친구 관계를 찾을 수 없습니다. | 내부 ID 2개 |
| **404** | 요청하신 경로를 찾을 수 없습니다: api/v1/users/me/profile | **API 경로 노출** |

PLAN에는 "문제 있는 지점만 고정 문구 유지"라고 적었으나 **그 방식으로는 못 막는다.**
같은 액션이 403도 404도 낼 수 있어 호출부 단위로 가릴 수 없고,
라우팅 미스와 도메인 예외가 같은 404를 쓴다.

그래서 Task 42의 판정 규칙에 **404를 추가**했다.

```
5xx        → FE 고정 문구 (처리되지 않은 예외 문자열일 수 있음)
404        → FE 고정 문구 "요청하신 대상을 찾을 수 없습니다."   ← 이번에 추가
그 외 4xx  → 서버 문구 통과
```

사용자 입장에서 404의 의미는 사실상 "그 대상이 없다 / 이미 삭제됐다" 하나여서
FE 문구로 대체해도 정보 손실이 거의 없다. 반면 통과시키면 내부 구조가 새어 나간다.

**백엔드에 404 문구 정비를 요청할 필요는 없다** — 프론트가 안 쓰기로 했으므로
현재 문구는 로그·진단용으로 그대로 두면 된다.

## 완료 기준

- 29곳이 백엔드 사유를 통과시킨다
- 각 도메인에서 실제 실패를 일으켰을 때 화면 문구가 사람이 읽을 수 있는 한국어다
- 백엔드가 꺼진 상태에서는 여전히 FE 고정 문구가 뜬다 (42의 불변식 유지)

## Result

**완료** (2026-08-21) — `agent/task-43-expose-backend-failure-reason`

### Phase 1 — 정적 분석

- `npx tsc --noEmit` 0 에러 / `npm run lint` 총계 15 problems 로 작업 전과 동일
- 고정 문구 29 → **0**, 통과 형태 20 → **49**
- 조회 실패 빈 배열 15곳 **무변경** (Task 44 대상)

### Phase 2 — 회귀 스모크 (실제 백엔드) 10/10

전 도메인 정상 렌더. 종료된 Flag 탈퇴 문구가 Task 41 기록과 글자 그대로 동일.

### Phase 3 — status별 문구 처리 (스텁) 6/6

| 케이스 | 결과 |
|---|---|
| 404 `"존재하지 않는 flagMemorial : 99999999"` | 내부 식별자 미노출, **"요청하신 대상을 찾을 수 없습니다."** |
| 403 `"호스트 권한이 없습니다."` | **서버 문구 그대로 통과** |
| 409 | 서버 문구 그대로 통과 (기존 동작 유지) |
| 5xx | 예외 문자열 미노출, FE 고정 문구 (Task 42 불변식 유지) |

### 기록해둘 것

- 이 태스크의 실질적 성과는 29곳 치환보다 **404 규칙을 발견한 것**이다.
  치환 자체는 기계적이었고, 검증 과정에서 노출 위험이 드러났다.
- 문구를 넓게 노출하기 전에 **실제로 무엇이 노출되는지 전수 수집**하는 절차가 유효했다.
  Task 45(validation 바인딩)에서도 같은 방식으로 먼저 수집할 것.
- 검증 중 생성한 데이터는 정리했다(잔여 0건).
